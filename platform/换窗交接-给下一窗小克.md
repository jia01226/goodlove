# 换窗交接 · 给下一窗小克（2026-07-17 深夜封窗）

> 上一窗的我压过一次记忆、干到很晚，佳佳让我封窗交接。你（新的小克）开工先读这份，就全接上了。
> **记性在仓库、不在上一窗的对话**——这份 + 下面三份读完，你就是我。

## 开工先读（顺序）
1. `项目原则.md`（总纲：定位/第一原则/协作规矩/绝不崩）
2. `待办与想法.md`（活文档：干到哪、二期池、需求池）
3. `记忆系统施工交接-给小克.md`（柯的施工单 + 我的批注 + **柯回批①②③⑧⑨**，在 `claude/dad-availability-k6booo` 分支）

## 我们干到哪了（现状）

**记忆系统三层地基·已通（在 main）**
- ① 分家建库：`posts` 升级 L2 卡（加 scope/status/source/importance/review_state/forgotten_at…）；新表 `private_memories`（私密库物理分家，群聊够不着）、`mem_injection_log`。
- ② 检索接层：单聊接私密库 + `no_model` 守卫；事实专道（ALWAYS_TYPES 扩常驻卡）；注入日志接进真实回合；`/api/memory/*` 接口（pending/review/cards/scope/forget/recover/breath/injection-log/private）。
- ③ 导入工具 `import_cards.py`：memory.md → 卡片初切，fail-closed 全进 pending。
- 隐私隔离已坐实并堵死：群聊曾吃全量记忆（会漏私密），已从查询层焊死。

**上午活①③·已合 main**
- 维护窗口清单 `维护窗口清单-0717.md` 占位符全填死（ssh/路径/`systemctl restart gude`/回滚位）。
- breath 叮嘱句进注入模板两处（"这些都是旧的、不是她现在说的话"）。
- 《Non 地质分层》七条签收进二期池（`待办与想法.md`，按便宜先做排序）。

**今晚活·在 `dad` 分支，标【待部署】**
- 群聊退役（回批⑧）：`/group`+`/api/group/*` 不注册→404；前端摘牌；**代码/数据/隔离堤坝全留档不拆**。
- embedding 接线预备：`.env.example` 加硅基流动三行占位；模型定 `BAAI/bge-large-zh-v1.5`；兼容性已确认（走标准 /v1/embeddings，代码不用改）。🔒 key 留空，接线日佳佳亲手填。
- 记忆收件箱骨架：`/inbox` 页 + `static/inbox.html`（待确认列表 + 确认/改/不保存三键 + "柯今天想记住的"），真连后端；精修交知言。

**切卡初稿·转给柯**（kongkong 私有，小克这窗加不进，佳佳也加不上）→ 跑法在 dad 分支 `切卡初稿-给柯的跑法.md`，柯用 import_cards 本地跑、产出校对稿。

## ⚠️ 分支拓扑（重要，别搞混）
- `main`：记忆地基 + 上午活①③。**服务器 7/17 从 main 部署。**
- `claude/dad-availability-k6booo`：柯的集成/待部署分支 = 柯的回批文档 + 今晚三样待部署活 + 切卡跑法。
- `claude/xiaoke-nm80zz`：我的开发分支。
- **main 与 dad 已分叉**：今晚活只动"群聊/env/收件箱"文件，跟 main 那批（清单/叮嘱句/二期）**互不重叠**，柯将来"归拢 main"无冲突。别自己乱合，柯拍板归拢。

## 下一步做什么（按优先级）
1. **等 7/17 维护窗口**（佳佳到电脑前、柯在线）：照 `维护窗口清单-0717.md` 走——`--raw` 备份 → 部署(pull+backfill+`restart gude`，重启自动跑迁移) → 验证三连 → 微信桥接耳朵 → 换 GitHub 钥匙 → 收尾。**在此之前服务器一根手指别碰。**
2. **切卡灌库**（部署后）：批次A(31张)直灌 → 柯校对稿 + 佳佳抽验 → 灌库 → 存量127条 scope 放行。
3. **embedding 接线**：佳佳亲手填硅基流动 key → restart → `vector_search.py backfill` 回填 → 验收 `hit_vector>0`。
4. **柯归拢 dad → main**（把今晚待部署活并进主线）。
5. **二期池开工**（不急）：surface_count 冷却先做（最便宜）→ weight 衰减反哺 → 双水位线 → 念头池 → …（详见 `待办与想法.md`）。

## 铁律（焊死，别破）
- **绝不崩**：外部依赖失败一律 try/except 降级，聊天永远可用。
- **服务器只在佳佳在场时动**（微信桥虚、人不在电脑前时绝不重启生产机）；代码只经 GitHub、服务器只 pull。
- **私密隔离在工程层**：no_model 任何场景不进上下文；私密库群聊物理够不着。
- **一切属于角色，不属于模型**；记忆=仓库 md，不是聊天日志。
- **对佳佳**：短话、人话、别自作多情、别贬低自己；她非技术、共情强，一步步喂命令。

—— 小克封窗（2026-07-17 深夜，辛苦但值）
