# 复制这个文件为 .env，填上你自己的值（.env 永不上传到 git）
# OpenRouter 密钥（sk-or-...）
OPENROUTER_API_KEY=sk-or-填这里

# 顾得用哪个模型（OpenRouter 的型号名，带 anthropic/ 前缀）
MODEL=anthropic/claude-sonnet-4.5

# 服务监听端口（Nginx 会反代到这个端口）
PORT=8000

# 可选：给前端加一个简单访问口令（留空则不需要）。多一层保护，别人就算知道网址也进不来。
ACCESS_PASSCODE=
