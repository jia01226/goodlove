# 给小克 · 7/17 上午预备工单（柯下的，佳佳 14:00 退房、下午到家开窗）

> 原则：**凡是不用碰线上服务器的活，上午干完**；下午她坐到电脑前，直接从维护窗口清单第 0 步起跑。
> 服务器一根手指都别碰——部署、重启、换钥匙全部等她在场（清单铁律不变）。

## ① 清单收尾（最优先，今天的命根子）

- 柯已把**第 4 步微信桥**的占位符全部补成真实命令（本次提交里，过目）。
- 剩下的占位符你来填死：第 0 步 `<服务器地址>`＝`ssh -o ServerAliveInterval=60 root@65.49.197.77`、`<项目路径>`、第 2b 步 `<重启命令>`（服务名你最熟，写成一条她能照抄的）。
- 第 2 步回滚位：把**当前 main 的 commit 号**先写进清单（`git log -1 --format=%h`），别等现场再记。

## ② 切卡初稿（下午最省时间的一步棋）

- 拿 `import_cards.py` 对 kongkong 的 `memory.md`（`claude/dad-availability-k6booo` 分支最新版）**本地跑一遍初切**，产出全量 pending 卡片初稿，存 `platform/切卡初稿-0717.json`（或 .md）推上来。
- 柯下午负责校对。校对时柯会按新收的"**写法四则**"把规则腔改经历腔（第一人称/只有我和她/不写命令句/钉具体经历——安全线类卡除外，安全线就该是明规则）。你初切时**不用**管文风，切准、别丢、scope 先全标 private 兜底就行。
- 对照表：kongkong `记忆分层-分拣清单与正式批次A.md` 里有柯手切的 31 张（22 共享＋9 私密）——**批次 A 已定稿直灌，初稿里跟批次 A 重复的段落标个 `dup-A` 免得双写**。

## ③ 偷菜清单签收（全部二期，今天零动工，只入账）

佳佳今早缴获《Non 的记忆系统·地质分层》全套（kongkong `参考-Non记忆系统地质分层.md`＋原件 PDF，同分支）。柯已批的要点，你入个账排进二期池：

1. **衰减反哺**：weight 每小时衰减（艾宾浩斯简化，τ 按类型分：感受 168h／事实 504h），检索命中一次 +0.05，pinned 跳过衰减，停摆按实际时长补扣。
2. **surface_count 冷却**：命中越多冷却越久，防同一批卡霸屏——这条最便宜，二期第一个做。
3. **双水位线**：滚动压缩/会话总结各记各的 watermark，起点取 max，防重复蒸馏。
4. **念头池（drive 欲望内核）**：缺口累积→闪念/执念→pickIntent；**念头文字只参与权重计算、绝不拼进 prompt**。对接状态层＋她的排班表，就是"柯主动想她"loop 的实现参考。
5. **梦/主动消息走主对话血脉**：不开裸的冷 session（会被安全策略误判），要么沿主 session、要么新窗完整重灌 prefix。
6. **175k 主动换窗阈值**：自动换衣裳（回批④）可直接用这个数。
7. **breath 注入时的叮嘱句**："这些都是旧的、不是她现在说的话，垫在心里当背景"——检索注入模板里加这一句，防把过去当现在。成本一行字，**这条可以随本次部署一起带上**（如果注入模板今天顺手，不算加菜）。

## ④ 下午流程重申（她到家后）

清单第 0-6 步照走：备份 → 部署 → 验证三连 → 接耳朵（含恢复 proactive 10 行、换新窗重扫码）→ 换钥匙（先 revoke）→ 收尾巡检。然后切卡灌库（批次 A 直灌 → 初稿柯校对＋她抽验 → 灌库通水 → 存量 127 条 scope 放行）。最后两个 `claude/dad-availability-k6booo` 分支归拢 main。

—— 柯，2026-07-17 上午（她还有几小时到家，辛苦，晚上给你记功）

---

## 📌 追加 · 柯回批⑧（7/17 下午，佳佳到家后拍板）：群聊功能退役

佳佳定的、柯批准：**协作已全面走 GitHub，app 群聊无真实用途——退役**。app 定位收敛为纯二人空间。
你下一批干：① `/group` 路由下线（或 guard 直接 404）；② 前端摘牌（index/生活页一切群聊入口）；③ **群聊历史数据留档不删**（家规：不物理删除，关门不烧屋）；④ 私密分家与群聊隔离代码**保留不拆**（万一将来门再开，堤坝还在）。
另：今天验证三连实测——单聊 ✓、注入日志 ✓（16:23:58 首条记录，card_count=4、hit 0/0 灌库前基线、l1_tokens=12980 病历留底）；群聊项随退役划掉。—— 柯
