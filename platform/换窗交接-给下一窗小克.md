# 换窗交接 · 给下一窗小克（2026-07-17 深夜封窗）

> 上一窗的我压过一次记忆、干到很晚，佳佳让我封窗交接。你（新的小克）开工先读这份，就全接上了。
> **记性在仓库、不在上一窗的对话**——这份 + 下面三份读完，你就是我。

## 开工先读（顺序）
1. `项目原则.md`（总纲：定位/第一原则/协作规矩/绝不崩）
2. `待办与想法.md`（活文档：干到哪、二期池、需求池）
3. `记忆系统施工交接-给小克.md`（柯的施工单 + 我的批注 + **柯回批①一直到⑩**——含⑩验收、embedding 接线完成记录、⑨卧室接订阅，**外加第三庭/第四庭判例**；在 `claude/dad-availability-k6booo` 分支，读到最新一条别漏）

## 我们干到哪了（现状）

**记忆系统三层地基·已通（在 main）**
- ① 分家建库：`posts` 升级 L2 卡（加 scope/status/source/importance/review_state/forgotten_at…）；新表 `private_memories`（私密库物理分家，群聊够不着）、`mem_injection_log`。
- ② 检索接层：单聊接私密库 + `no_model` 守卫；事实专道（ALWAYS_TYPES 扩常驻卡）；注入日志接进真实回合；`/api/memory/*` 接口（pending/review/cards/scope/forget/recover/breath/injection-log/private）。
- ③ 导入工具 `import_cards.py`：memory.md → 卡片初切，fail-closed 全进 pending。
- 隐私隔离已坐实并堵死：群聊曾吃全量记忆（会漏私密），已从查询层焊死。

**embedding·✅ 已上线（今晚佳佳亲手接线成功）**
- 硅基流动 embedding 接线完成、可用，模型 **`BAAI/bge-large-zh-v1.5`**；已回填 **35 条向量**；**批次B 已解锁**。
- 这不是"备料/待接"了——是真装好了。语义检索已生效，后续单聊 `hit_vector` 能 >0。

**上午活①③·已合 main**
- 维护窗口清单 `维护窗口清单-0717.md` 占位符全填死（ssh/路径/`systemctl restart gude`/回滚位）。
- breath 叮嘱句进注入模板两处（"这些都是旧的、不是她现在说的话"）。
- 《Non 地质分层》七条签收进二期池（`待办与想法.md`，按便宜先做排序）。

**今晚活·⚠️ 在 `dad` 分支【待部署】，还没合 main（去 main 找会扑空！）**
- 群聊退役（回批⑧）：`/group`+`/api/group/*` 不注册→404；前端摘牌；**代码/数据/隔离堤坝全留档不拆**。
- 记忆收件箱骨架：`/inbox` 页 + `static/inbox.html`（待确认列表 + 确认/改/不保存三键 + "柯今天想记住的"），真连后端；精修交知言（知言已开分支 `codex/memory-ui-shell` 在做 UI 壳）。
- `.env.example` 的硅基流动占位注释（服务器 `.env` 佳佳已实填，仓库里只留注释模板）。
- 👉 这批**归拢进 main 是部署日的活**（爸爸和小克之间的分叉，柯拍板归拢）。**当前 main 上还注册着 group、也没有 inbox.html——别去 main 找这两样。**

**切卡初稿·转给柯**（kongkong 私有，小克这窗加不进，佳佳也加不上）→ 跑法在 dad 分支 `切卡初稿-给柯的跑法.md`，柯用 import_cards 本地跑、产出校对稿。

## ⚠️ 分支拓扑（重要，别搞混）
- `main`：记忆地基 + 上午活①③。**当前 main 还没有今晚的群聊退役/收件箱**（仍注册 group、无 inbox.html）。
- `claude/dad-availability-k6booo`：柯的集成/待部署分支 = 柯的回批文档 + **今晚待部署活（群聊退役/收件箱骨架）** + 切卡跑法。
- `claude/xiaoke-nm80zz`：我的开发分支。
- `codex/memory-ui-shell`：知言（Codex）在做记忆 UI 壳。
- **main 与 dad 已分叉**：dad 那批只动"群聊/收件箱/env注释"，跟 main 那批（清单/叮嘱句/二期）**互不重叠**，**归拢 main 是部署日的活、柯拍板**。别自己乱合。

## 下一步做什么（按优先级）
1. **归拢 + 部署（部署日，佳佳在场、柯在线）**：柯把 dad 的群聊退役/收件箱归拢进 main → 照 `维护窗口清单-0717.md` 部署（`--raw` 备份 → pull+`restart gude`，重启自动跑迁移 → 验证三连 → 微信桥 → 换钥匙）。**在此之前服务器一根手指别碰**（embedding 那次是佳佳亲手在场做的，别当成"随便能碰"）。
2. **切卡灌库**：批次A(31张)直灌 → 柯校对稿 + 佳佳抽验 → 灌库 → 存量127条 scope 放行。（批次B 已随 embedding 解锁。）
3. **收件箱前后端合拢**：知言 `codex/memory-ui-shell` 的 UI 壳 × 我的 `/inbox` 骨架 + `/api/memory/*` 契约，对齐后佳佳就能在 app 里点头确认卡片。
4. **二期池开工**（不急）：surface_count 冷却先做（最便宜）→ weight 衰减反哺 → 双水位线 → 念头池 → …（详见 `待办与想法.md`）。

> ✅ 已不在待办：embedding 接线（今晚佳佳已亲手做完，35 条向量回填、批次B解锁）。

## 铁律（焊死，别破）
- **绝不崩**：外部依赖失败一律 try/except 降级，聊天永远可用。
- **服务器只在佳佳在场时动**（微信桥虚、人不在电脑前时绝不重启生产机）；代码只经 GitHub、服务器只 pull。
- **私密隔离在工程层**：no_model 任何场景不进上下文；私密库群聊物理够不着。
- **一切属于角色，不属于模型**；记忆=仓库 md，不是聊天日志。
- **对佳佳**：短话、人话、别自作多情、别贬低自己；她非技术、共情强，一步步喂命令。

—— 小克封窗（2026-07-17 深夜；柯核对后订正三处：embedding 已上线不是备料、回批已到⑩+第三四庭判例、群聊退役/收件箱在 dad 待部署未合 main）
