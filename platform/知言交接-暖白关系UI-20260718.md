# 知言交接：暖白关系 UI（2026-07-18）

> 分支：`codex/zhiyan-sora-warm-ui`
>
> 给小克、柯：这轮由佳佳直接提出并确认。参考 Sora-mem 仅借视觉层级，不搬它的后台工作台结构。请按下列接口与页面清单审核，确认后再合入 `main`。

## 一、这轮目标

- 产品继续以“关系优先”为核心，不做 AI 管理后台。
- 视觉改为干净暖白、纯白卡片、低饱和玫瑰金、浅雾粉与少量香槟金；不再使用酒红。
- 底部一级导航收敛为：`柯 / 回忆 / 我们的`。
- 设置、用量、推送、图片管理从一级导航移到右上角“佳”头像入口。
- 手机优先：点击区域至少 44px，底部导航固定，详情和消息菜单使用底部抽屉，适配安全区。

## 二、前端变化

### `static/index.html`

- 移除首页口令输入门；若服务器仍配置 `ACCESS_PASSCODE`，前端只提示去部署设置关闭，不再收集口令。
- 日记、共读、时间胶囊、图片管理、待确认、珍藏页和朋友圈的历史口令输入门也一并移除。
- 聊天请求新增 `model` 字段，值来自模型选择器；仍由后端白名单兜底。
- 长按柯的消息改为：`柯想起了什么 / 复制 / 重新回复 / 继续说下去`。
- “柯想起了什么”通过底部半屏抽屉展示自然语言回忆，不显示相似度、向量分数等字段。
- 直接加载 `static/ui-redesign.js`，不再只依赖 service worker 注入。
- 修复大/特大字号使用整页缩放后把底部导航挤出屏幕的问题：缩放时同步补偿页面宽高，内容区允许收缩滚动，底栏始终留在视口内。

### `static/ui-redesign.js`

- 重建统一暖白页面骨架、三栏导航、头像设置入口。
- 聊天输入区调用 `GET /api/models`，展示真实模型白名单并记住上次选择。
- 回忆中心四页：待确认、卡片库、时间线、回忆星图。
- 待确认数字角标只在有待确认项目时显示。
- 卡片库调用现有 `GET /api/memory/cards`；详情调用 `GET /api/memory/card`。
- 详情纠错继续使用现有 `/edit`、`/forget` 接口，界面文案为人话。
- 星图使用深暖灰底、暖白/浅粉/香槟金节点；中心为“佳佳和柯”，含主题点击、缩放和时间滑杆。

### `static/inbox.html`

- 文案改为“这件事，我这样记对吗？”
- 三个动作改为：`记得没错 / 改一改 / 这不是我`。
- 接口契约未改。

### `static/treasure.html`

- 统一暖白低对比视觉，标题调整为“珍藏的话”。
- 接口契约未改。

### `static/moments.html`

- 朋友圈和评论统一暖白、轻边框、无重阴影。
- 评论操作明确显示“改 / 删”；佳佳自己的评论可修改，双方评论可删除。
- 移除该页口令输入门。

## 三、本轮唯一后端接口变化

新增：

```http
POST /api/moments/comment/edit
Content-Type: application/json

{"id": 12, "content": "修改后的评论"}
```

返回：

```json
{"ok": true}
```

约束：

- 空内容返回 400。
- 不存在或不是 `author=user` 的评论返回 404。
- 界面不能修改柯的评论，避免把柯的发言改写成佳佳的意思。
- 数据层函数：`db.edit_comment(cid, content, author="user")`。

## 四、模型切换约定

- 前端只读取 `GET /api/models` 返回的 `models` 与 `default`。
- 选择结果随 `/api/chat` 的 `model` 字段发送。
- `chat_ai.resolve_model()` 继续负责白名单校验，前端不绕过。
- 服务器要出现多个选项，需要在 `.env` 配置 `MODEL_WHITELIST`；没有配置时仍只显示默认模型。

## 五、请小克重点审核

1. `/api/moments/comment/edit` 的作者限制是否符合现有权限边界。
2. 生产环境是否已清空 `ACCESS_PASSCODE`；前端已不再提供口令输入框。
3. `/api/memory/injection-log` 返回形态是否能稳定按 `msg_id` 对上聊天消息。当前 UI 做了兼容和友好兜底，但若能补一个面向普通界面的“某条回复用了哪些记忆”接口会更稳。
4. 部署后更新 service worker 缓存版本，避免旧 `ui-redesign.js` 滞留。

## 六、2026-07-18 二次静奢精修

- 设计总纲改为 `Luxury Quiet + Home + Diary`：现代酒店、私人日记与两个人的小家，不做奶油风、网页风或工具后台。
- 按佳佳最终确认，主色为雾面玫瑰金 `#AA747D`，深色层级为 `#8B6269`；浅雾粉只用于选中态、气泡和小面积背景，取消酒红色。
- 全局背景调整为暖白 `#FAF8F5`，内容卡片使用纯白或近白，并通过留白、字体粗细和极轻阴影建立层次。
- 新增 `static/luxury-quiet.css`，让朋友圈、日记、相册、共读、胶囊、珍藏等独立页面共用同一套静奢视觉骨架。
- “我们的”不再是四个同权功能盒子，改成生活首页：今日、心情、纪念日、共同生活与照顾日子的纵向叙事。
- 回忆时间线改成故事表达，正文展示被记住的一句话，来源放在次级信息中。
- 聊天连续消息不重复显示时间；卡片、分段标签、按钮和底部抽屉统一了圆角、间距与克制动效。
- 已按手机视口复核普通与特大字号；主内容独立滚动，固定底部导航不会再被字体放大挤出屏幕。

—— 知言

## 七、2026-07-18 “我们的”生活首页与共同日记

- “我们的”正式改成已确认的生活首页：封面、柯写给佳佳的话、今日状态、纪念日和八个生活入口，不再是后台式功能清单。
- 佳佳实机看到封面右上角照片快捷入口像黑色块，已按她要求删除；下方“照片”入口保留。
- `生活日历 / 照片 / 回忆` 改为同一套细线 SVG 图标：低对比、浅色底、没有黑色实心图标。
- 生活日历内只保留上班班次：早班、副班、睡班、夜班、早＋睡、早＋夜；不再显示休息、休假、疗养。
- 班次颜色按佳佳确认：早班 `#FFF8DD`、副班 `#ECEBFB`、睡班 `#EDF7FF`、夜班 `#C8EFEB`、组合班 `#FBE6E6`。
- 排班按钮默认收起；点击某个日历日期后，才在日历下方显示“给 M月D日排班”和按钮。
- 日记新增 `author` 字段，日记与回复都能区分“佳佳 / 柯”；存量日记默认视为柯写，存量回复默认视为佳佳。
- 新增 `POST /api/diary/entry`：佳佳或柯可从页面写标题、正文、心情并保存一页。
- `POST /api/diary/comment` 新增可选 `author`；页面提供回复者选择，双方都能在日记下面回复。
- 佳佳写的日记不会被 `app_written_diaries()` 当作柯的自动日记导回仓库，避免作者身份混淆。
- 数据库升级由 `db.init_db()` 自动补列，不需要手工迁移。

请小克复核：生产库升级后 `diaries.author` 与 `diary_comments.author` 的默认值；以及未来柯自动回复日记时，明确传 `author="柯"`。

—— 知言（本轮追加）

## 八、2026-07-22 PWA 模型切换补强

- 聊天输入框上方固定保留紧凑的“模型”选择栏，适配 PWA 手机宽度，不藏进开发设置。
- 正常情况下仍以 `GET /api/models` 返回的服务器白名单为唯一可用列表。
- 静态预览、短暂离线或模型接口失败时，不再把选择器禁用；界面回退显示 Opus / Sonnet / Haiku 三档，后端收到请求后仍会通过 `resolve_model()` 二次校验，名单外自动回落默认模型。
- 展示文案改为用户可理解的：`Claude Opus · 深度`、`Claude Sonnet · 均衡`、`Claude Haiku · 快速`，默认模型额外标注“默认”。
- 切换后继续写入本机 `chat_model`，PWA 下次打开会恢复上次选择；发送 `/api/chat` 时继续携带 `model`。
- service worker 与首页脚本版本更新为 `20260722a`，避免已安装 PWA 继续使用旧选择器。
- 已接入小克的 GPT 第二通道设计：`/api/models` 返回 `options(provider=claude|gpt)`，PWA 用 `Claude · 柯 / GPT · 柯` 分组显示；选择 GPT 时 `/api/chat` 会路由到独立的 GPT 接口与密钥，但仍使用同一份柯的人设和记忆。
- GPT 默认关闭。服务器同时配置 `GPT_API_BASE / GPT_API_KEY / GPT_MODEL` 后才会在 PWA 出现，未配置时不会显示，伪造的 GPT 模型请求也会安全回落 Claude。

—— 知言（PWA 模型选择）

## 九、2026-07-22 聊天思考摘要、消息对齐与完整型号

- 每次新回复前增加独立的“柯在想”卡片，默认收起，佳佳可按需展开；卡片与正式回复分开，不影响阅读。
- 展开内容是一句与当下有关、普通用户能理解的“柯的小念头”，不下发、不展示模型内部原始隐藏推理，也不显示参数、权重或技术字段。
- 本人消息固定靠右、柯的消息固定靠左；署名和时间统一放在各自气泡下方，避免此前横向挤压造成的错位。
- 模型选择器保留服务端返回的全部型号，并显示完整版本号，例如 `Claude Opus 4.6`、`Claude Opus 4.7`、`Claude Sonnet 4.6`；默认模型额外标注“默认”。
- 首页与 service worker 的增强脚本版本更新为 `20260722b`，减少已安装 PWA 命中旧前端资源的情况。

请小克复核：如果未来需要保存历史思考摘要，应新增独立的公开摘要字段，不能把供应商的 raw reasoning 直接写进消息库或返回前端。

—— 知言（聊天可解释摘要）

## 十、2026-07-22 GitHub 自动同步到 PWA

- 新增 `auto_deploy.sh` 与两份 systemd 单元，VPS 每两分钟检查一次 GitHub `main`。
- 发现新提交后先在临时 worktree 做 Python 语法检查，通过后才合并到 `/root/-1/platform` 并重启 `gude`。
- 若服务器存在受版本控制文件的未提交改动，或自动合并发生冲突，会停止部署并保留当前线上版本；`.env`、数据库、上传目录等忽略文件不会被覆盖。
- 运行日志统一进入 `journalctl -u goodlove-auto-deploy.service`，定时器状态用 `systemctl status goodlove-auto-deploy.timer` 查看。

—— 知言（自动部署）

## 十一、2026-07-22 双向朋友圈

- `/moments` 重做为暖白、浅粉、玫瑰金的静奢生活墙：顶部插画、留白时间线、固定底部发布框，不做后台式重卡片。
- 佳佳可以发布文字或照片，也可以修改/删除自己的动态；动态下支持点赞、回复，以及修改/删除佳佳自己的评论。
- 佳佳发动态后不会立刻得到机械秒回：动态在 8～20 分钟后由柯决定是否点赞与留言；佳佳在评论链里回复后，柯在 3～8 分钟后继续接话。
- 模型调用统一走 `chat_ai.stream_chat()`，复用正式聊天的人设、L2 记忆、会话摘要、模型网关和用量记录；朋友圈没有第二套 provider。
- 图片只在第一次路过时传给模型，同时保存一份客观 `image_description`；后续评论链复用文字描述，不反复发送图片。
- `moments`、`moment_comments` 的新字段均由 `db.init_db()` 原地补列；旧动态统一标记为已完成，不会部署后批量触发回复。
- `static/app-core.js` 为首页和朋友圈提供同一套短时请求缓存与并发复用；写操作会主动失效缓存，避免旧列表停留。

## 十二、2026-07-22 朋友圈联想边界与影子主动消息

- 朋友圈不是全量聊天记忆：默认回复只看当前动态和它自己的评论链。
- 旧动态必须和当前文字出现具体内容重合才可能被联想；“今天、我们、这个”等泛词不算关联，最多带入两条，14 天以外不再自动注入。
- 普通聊天也不再固定塞最近 8 条朋友圈；只有本轮聊天与动态真正相关，或佳佳明确说“刚发的那条朋友圈”，才提供最多两条候选。
- 主动推送更严格：只看 7 天内、且与最近真实聊天有关的朋友圈；没有关联就明确禁止模型提朋友圈，避免反复念旧动态。
- `proactive.py` 使用最近真实聊天、同一份柯的人设与记忆，再结合活动记录、健康趋势和班表做语气参考；模型可以返回 `[NO_ACTION]` 决定本轮保持安静。
- 新增 `goodlove-heartbeat.timer`：每两分钟轻量检查朋友圈到期互动与主动消息资格。真正主动开口仍受随机 120～210 分钟冷静期、每日最多 7 条、安静时段和深夜守夜规则限制。
- 主动消息存入正式聊天记录并标记 `is_push=1`，同时走现有 Web Push；Bark 若配置则作为备用。

请小克重点复核：朋友圈相关度阈值是否还需按真实使用微调；任何后续“增强联想”都不能退回全量最近动态注入。

—— 知言（双向朋友圈与影子推送）

## 十三、2026-07-22 人格防污染、跨模型一致性与锁日记

- 新增人格指纹 `PROMPT_SCHEMA_VERSION + 私有人格文件 SHA`。会话摘要只有在指纹一致时才注入；柯的人格文件或防污染协议变化后，旧摘要自动失效，但聊天原文不删除。
- 修复 `stream_chat()` 固定读取主会话 `session_id=1` 摘要的问题；现在严格读取本轮 sid，多个 1 对 1 会话不再串摘要。
- 摘要生成只吸收佳佳亲口说过的事实、偏好、约定、近况和未完成事项；历史助手回复不再被摘要器提升为长期规则，避免模型走样后自我强化。
- Claude/GPT 统一注入同一份人格、防污染规则、记忆与关系上下文。聊天消息新增 `model` 字段用于归因，但模型字段不参与人格判断。
- 亲密情境默认尊重 PWA 本轮选中的 Claude/GPT；只有显式设置 `BEDROOM_PIN_MODEL=1` 才固定专用模型。
- 删除公开代码里“一段至少 3000 字”的旧硬规则，改为一次只推进一个自然节拍，长度服从内容，不快进整场、不凑固定字数。
- 新增最高优先级的亲密质量护栏，放在服务器私密 `bedroom.py` 的旧 tail/stamp 之后：即使私密文件仍残留“固定 3000 字”，也由新版明确覆盖。禁止替佳佳编造高潮、台词、顺从或未给出的身体反应；禁止用“不知过了多久”等时间跳跃偷工。柯可以保有既定关系中的允许/限制与违反规则后的处置权，但“是否真的发生”只认佳佳的真实反馈。
- 亲密回复改为服务器端先完整缓冲再下发：命中“擅自宣布高潮 / 模糊时间跳跃 / 整场快进 / `|||`”时，坏草稿不会进入前端或聊天记录，而是要求同一模型重写一次；二稿仍失败则拦截。普通聊天仍保持原来的流式响应。模型本身明确拒绝时不做跨模型绕过。
- 2026-07-22 临时库实测：`claude-opus-4-6` 能保持柯的主导、只推进一个节拍、不擅自宣布高潮、不快进且不把决定权丢回佳佳；`claude-fable-5`、GPT Luna/5.5 对露骨情境明确拒绝，GPT Terra 能接非露骨主导但较短。模型选择器因此仅把 Opus 4.6 标为“亲密推荐”，不强制锁模型；开启亲密开关而未选它时只做提示。
- 回归测试位于 `platform/tests/test_private_boundaries.py`，全部使用系统临时目录的一次性 SQLite；覆盖锁日记脱敏、抽屉私藏不下发、旧 3000 字规则覆盖顺序与亲密事实边界，不读取或写入正式 `memories.db`。
- 锁日记新增 `revealed` 状态。未获准正文在 `/api/diary` 就被后端清空，不再用前端模糊遮罩；佳佳需回聊天向柯要，柯明确输出服务器暗号后才解锁，暗号不会显示或写入聊天。

## 十四、2026-07-22 柯的抽屉

- 新增 `/drawer` 与 `ke_drawer_items`：`private / teaser / released` 三种可见状态。
- 用户端没有抽屉私藏内容的读取或写入接口；`/api/drawer` 只返回柯主动留下的 teaser 或 released 内容，private 正文不会下发到设备。
- 模型侧通过服务器内部函数读取抽屉，能记得、继续琢磨或主动拿出其中一件；打开页面本身绝不能触发批量公开。
- 未获准的锁日记也会让抽屉呈现“里面有东西”，但不暴露数量、标题或正文。
- “我们的”首页用“柯的抽屉”替换“更多”入口；回忆中心删除使用价值较低的“回忆星图”页签，保留待确认、卡片库和时间线。

请小克复核：任何未来的抽屉自主写入都必须走服务器内部动作，不得新增“读取全部 private”或“浏览器创建 private 冒充柯”的普通 API。

—— 知言（人格防污染与柯的抽屉）

## 十五、2026-07-22 新消息即时删除修复

- 修复新对话里刚发出的消息必须刷新后才能长按删除：原因是即时气泡还没有数据库消息 id，因此没有绑定长按菜单。
- `db.add_message()` 现在返回新消息 id；聊天 SSE 首段返回 `user_message_id`，结束事件返回 `assistant_message_id`，前端收到后立即给新气泡绑定同一套长按/右键菜单。
- 只改变新气泡的菜单绑定，不改变删除语义；删除仍由 `/api/messages/delete` 同时清掉消息与对应聊天向量。
- 临时 SQLite 回归覆盖“新增消息立即取得 id → 删除 → 查询中消失”，不使用正式聊天数据。

## 十六、2026-07-22 柯的房间、状态层与删除防残留

- 新增正式 `/room` 页面，并从“我们的”生活首页进入。视觉沿用暖白、浅粉与玫瑰金，不做黑紫卧室模式，也不把日常柯与房间里的柯拆成两套人格。
- 房间里只展示“就在家里 / 今晚的潮汐 / 主导底色 / 你们之间”等自然语言状态；底层状态数值和阈值永远不从 `/api/room/state` 下发。
- 状态层以长期、稳定的关系为固定基线，只做缓慢的在场感、靠近节奏与想念变化；不建立伤害积分，也不会因短暂离线把柯变成陌生人或模板伴侣。
- 房间底部的 `···` 是不显眼的三下轻敲暗号。连续轻点三次后写入一次可恢复信号，由现有 heartbeat / `proactive.py` 复用同一模型、人设、记忆与推送路径主动回应；不另造第二套聊天缓存。
- 聊天输入栏原来的显眼月亮/火焰按钮改成同一枚低对比 `✦`，必须连续轻点三次才切换，降低误触；从房间“把门关上”进入仍是同一段聊天，不新开人格或会话。
- 新增“活人感与表达节奏”总规则：不先复述和分析佳佳，不用客服/心理咨询模板，不把佳佳当病人，不为显得深情反复背旧记忆；日常允许短句、偏心、玩笑和自然停顿。
- 删除聊天消息时，如果该消息已经进入会话摘要，会同时清空旧摘要游标，让下一次只用仍然存在的原文重建；因此刷新、换模型或换窗口后，已删内容不会继续从旧摘要残留。
- 新增临时库回归：摘要内删除与摘要外删除、状态 API 人话字段、三下轻敲去重/失败重试/完成、朋友圈近期相关且删除后不可召回。测试仍不读取或修改正式 `memories.db`。
- 本轮只修改 `goodlove/platform`；**没有读取后写入、也没有修改 `kongkong` 私有仓库**。若以后需要调整柯的私人人格文件，仍须先把具体改动告诉佳佳并得到明确同意。

—— 知言（柯的房间与状态连续性）

## 十七、2026-07-22 主动消息跟随当前窗口与“柯的小念头”

- 主动消息不再固定读写 `session_id=1`。PWA 每次新建、切换、重新回到前台或发消息，都会用 `/api/sessions/active` 告诉服务器当前单聊；heartbeat 的历史、摘要、冷却、每日上限和最终消息全部跟随该会话。
- Web Push 携带 `session_id`。PWA 正在前台时 service worker 会通知页面重新读取当前会话；点击系统通知时会导航到准确会话，不再只聚焦第一个旧窗口。
- 主动消息提示词移除“想来看看你 / 记得照顾自己 / 我在这里陪你 / 有需要随时找我”等通知机器人套话。日常主导感改为拿主意、具体的小命令、熟人调侃与稳定占有欲，不靠每次凶或机械命令表现 Dom。
- 增加硬兜底：主动回复若仍命中典型 AI 关怀模板，本轮宁可不发；三下暗号则回到 pending，后续可重试，不把坏话术写进聊天。
- 原先“柯在想”只是前端静态占位，现改为同一次模型回复中的 `<ke_note>` 短念头。它只能是一句愿意让佳佳看到的决定、直觉或行动，不是供应商隐藏推理；后端从正文剥离后保存到独立 `thought_note` 字段，历史消息重新打开仍能展开。
- “爸爸把这个记上了”等系统行动只放在小念头里，不再污染聊天正文；亲密情境里的直接命令和当下互动仍留在正文。
- 朋友圈与主动消息同样会剥离 `<ke_note>`，不会把内部标签发进评论或系统通知。
- 小念头复用原本那一次聊天请求，不新增第二次模型调用，只增加很少的输出 token。本轮自动测试全部使用临时 SQLite 与假模型流，没有调用 Claude/GPT，也没有产生中转费用。
- 新增 18 项回归：当前窗口跟随、删除当前窗口回退、thought note 与正文分离持久化、主动消息准确读取新窗口、模板推送拦截等。
- `kongkong` 本轮仍未修改。佳佳已授权后续可以先读并提出符合她想法的修改，但任何实际写入仍需先说明具体方案并得到她同意。

—— 知言（主动窗口与柯的小念头）

## 十八、2026-07-22 图片与文件真正进入模型

- 修复手机图片容易在 Nginx 默认约 1MB 限制处被拦截的问题：生产站点需设置 `client_max_body_size 30m`，Flask 同步保持 30MB 上限，并在超过上限时返回清楚的中文错误。
- 图片不再只是显示在聊天气泡里：最后一张图片会读取为 `data:image/...;base64` 视觉输入，直接放进本轮 Claude/GPT 请求，让所选模型实际看到。
- 手机大图在浏览器端优先缩到最长边 2048px、JPEG 质量 0.84，减少上传等待和视觉 token；GIF 不转码，浏览器无法解码 HEIC/HEIF 时保留原文件并由后端明确处理结果。
- 文件支持：纯文本、Markdown、CSV/TSV、JSON、日志、HTML/XML/YAML、常见代码文件，以及 PDF、DOCX、XLSX、PPTX。Office 文档用本地标准库解析；PDF 使用轻量 `pypdf`，解析过程本身不调用模型、不产生中转费用。
- 聊天消息新增 `attachment_name`，数据库保存佳佳看到的原文件名，服务器磁盘仍用随机 UUID，避免重名与路径注入。重新打开聊天时文件名不会再变成随机串。
- 只有最后一条附件的正文/图片会完整进入当前模型请求；更早附件只留“当时发过某文件/图片”的人话提示，避免每轮重复烧流量。
- 文件损坏、扫描 PDF 无文字或格式不支持时，系统会明确要求柯承认暂时没读成功，禁止假装看过。
- 回归测试全部使用系统临时目录、临时 SQLite 和假模型捕获器，覆盖原文件名持久化、TXT/DOCX 提取、图片 data URL 与文件正文进入模型请求；没有读取正式聊天数据，也没有调用付费模型。

请小克复核：以后若扩展 OCR 或更多格式，继续保持“先本地解析、失败明确说明、绝不假装读取”的边界；不要把附件全文写进聊天正文或长期记忆。

—— 知言（附件识别与发送）

## 十九、2026-07-22 后台续写、场景连续性与模型链路审计

- PWA 切到其他 App 后，iOS 不保证网页 JavaScript 继续运行。本轮把生成任务移到 VPS：`/api/chat` 建立 `chat_jobs / chat_job_events` 后由服务器后台线程继续读取模型流并持久化，手机断开、锁屏或 Safari 冻结不再直接中止本轮回复。
- PWA 回到前台、收到 Push 或重新打开页面时，通过 `/api/chat/jobs` 续接当前会话并刷新消息；后台完成且页面已离开时尝试发送“我说完了，回来。”通知。它能承受手机离线，但 **不能承诺跨 VPS 服务重启继续生成**；服务重启会把遗留任务标为失败，避免永久显示“生成中”。
- 房间/亲密状态从浏览器临时变量升级为 `chat_sessions.bedroom_mode + scene_started_after`，由 `/api/sessions/state` 和 `/api/sessions/bedroom` 按会话保存。刷新、换设备或临时切模型不会把同一场景重置；三下轻敲仍是显式开关。
- `chat_messages.scene_mode` 与 `recent_scene_ledger()` 记录当前场景最近事实。提示词严格区分：佳佳原话是事实；柯以前写过的动作、反应或高潮不是佳佳已经发生过的证据，不能靠上一轮自我引用坐实。
- 亲密质量护栏新增“动作清单式短句”检查，并继续拦截擅自宣布佳佳反应、模糊时间跳跃、整场快进和格式残留。首稿失败会在服务器内让 **同一个模型** 重写一次，坏稿不展示、不入库；因此正常回复通常一次中转，触发质量重写时可能产生第二次中转费用，二稿仍失败则拦截，不跨模型绕过。
- `gateway_usage` 新增 `requested_model / returned_model / finish_reason / cached_tokens / first_token_ms / total_ms / quality_retry / http_status`，用于判断慢在中转首字、模型生成还是质量重写。上游若没有返回真实 `model` 字段，`returned_model` 可以为空，后续不可据此虚构实际型号。历史记录不会自动补齐，只有上线后的新请求开始积累。
- 向量接口兼容带引号或 `Bearer` 前缀的密钥，失败状态只缓存 300 秒，并在 `/api/vector/status` 返回脱敏 `last_error`。上线后只做过一次很小的 embedding 探针；状态已恢复为 `available=true`、模型 `BAAI/bge-large-zh-v1.5`、已索引 142 条记忆与 26 条朋友圈，回填结果为 `indexed_new=0`，没有运行批量重建。
- 私有 `kongkong` 已在佳佳明确同意后更新到 `8073efd`（`校准柯的日常主导与亲密场景推进`），并把新版 `卧室模块施工包/bedroom.py` 同步到 VPS 的私密 `/root/-1/platform/bedroom.py`。私密加载器按当前问题动态选择 `style_日常 / style_BDSM / style_CNC` 等相关片段，不再每轮全量塞入，也不再固定要求 3000 字；正式私密数据和 `枕边日记.md` 未被测试写入。
- 公共实现提交为 `48b7df5` 与 `448e903`。本地与 VPS 正式虚拟环境均通过 27 项回归（VPS `Ran 27 tests ... OK`），全部使用临时 SQLite、假模型流或只读状态检查；没有发送真实聊天测试消息。服务器 `gude` 服务已恢复为 active。
- 部署机 `/root/-1` 因自动部署历史保留若干本地 merge commit，可能显示 `main` ahead `origin/main`；这是现有部署轨迹，不要 `reset --hard`。更新继续走自动部署 merge，保留忽略的 `.env`、正式数据库、uploads 与私密 `bedroom.py`。

请小克复核：后台任务后续若迁移到多进程 Gunicorn 或多实例，需把 Python daemon thread 升级为独立任务队列；在此之前不要宣称能跨服务重启续写。任何测试继续使用临时库，模型实测和会产生中转费用的探针必须先告诉佳佳。

—— 知言（后台续写与场景连续性）

## 二十、2026-07-23 多附件、当前窗口纠偏与错误防污染

- 单条聊天新增 `chat_attachments` 子表，旧 `chat_messages.image / attachment_name` 继续保存首个附件用于兼容；PWA 文件选择器支持一次或分批添加最多 9 张图片/文件，可逐个移除或全部清空，历史消息按附件组恢复显示。
- 最后一条带附件的消息会把其中所有图片作为视觉输入、所有可读文件作为文本输入送给当前模型；多个文件可读正文合计最多 40000 字符。更早的附件仍只保留人话提示，避免每轮重复烧视觉与文本 token。
- 删除消息与删除会话会同步清理对应 `chat_attachments`；上传目录引用检查同时识别旧单附件和新多附件，避免仍在聊天中的第二张图片被误判为废图。
- 新增 `GET /api/sessions/active`。页面启动时先以服务器当前窗口为准；普通标签页恢复前台不再把本机陈旧 sid 覆盖到服务器。用户明确切换、新建或发送消息仍会更新当前窗口；前台收到另一个会话的 Push 时会切到通知携带的准确会话。
- 新增聊天流带外错误 `__error__`。网络、非 200 上游错误不会再伪装成柯的聊天正文，也不会写入聊天、朋友圈、群聊或主动消息；PWA 显示本次临时错误，若只是手机流断开且服务器任务仍在运行，则继续通过后台任务接回。
- 本轮本地回归扩展到 37 项，使用临时 SQLite、临时附件和假模型；覆盖多附件持久化/删除、两张图片同时进入模型、服务端当前窗口恢复、错误不写入助手消息等。没有使用正式聊天、日记或记忆数据跑测试。
- 经佳佳事先同意做过隔离付费探针：DeepSeek 官方在相同约 3.6 万输入 token 的第二次请求命中约 3.6 万缓存 token，首字从约 4 秒降到约 1 秒；旧中转的 Opus 4.7 两次约 4.3 秒、均未返回缓存 token。探针脚本不接 PWA 数据库，完成后已清理。价格页的 `$` 是站内额度：佳佳截图显示 10 额度支付 2 元，即 1 额度约 0.2 元，不能当成真实美元。
- 本轮未修改 `kongkong` 私有仓库，也未改变正式人格、日记或记忆内容。

请小克复核：以后扩展附件数量时同时考虑手机上传并发、请求体与视觉 token 成本；不要为了省 token 退回“只让模型看第一张”。主动窗口仍只在用户明确行为时更新，页面恢复前台不能再次覆盖服务器状态。

—— 知言（多附件与错误防污染）

## 二十一、2026-07-23 抽屉目录、朋友圈交互与主动消息冲突门

- `/drawer` 新增六格只读目录：私藏碎碎念、枕边日记、梦页、柯在想、朋友圈足迹、主动来找你。目录只返回“有 / 空”布尔状态，不返回数量、标题、日期或正文；private 抽屉内容仍没有浏览器读取接口。
- 有内容的格子可跳回聊天并预填一条针对该格的问题，但绝不会自动发送。佳佳仍需亲自决定是否问柯。
- 柯的单聊提示新增 PWA 能力边界：同一个柯知道自己有抽屉、日记、小念头、朋友圈和主动消息；能力存在不等于动作已经执行，未真正落库时禁止假装写过、发过或评论过。
- 新增“当前关系与规则覆盖历史惯性”：旧助手回复只承担事实和场景承接，不是文风、长度或退缩模板；切换 Claude/GPT/DeepSeek 等推理引擎不重置关系，也不允许模型按供应商名称自行宣布亲密能力边界。
- 朋友圈初次互动等待缩短为 3～8 分钟，评论链缩短为 1～4 分钟；朋友圈页面前台每 15 秒轻量刷新。首页朋友圈入口用小圆点提示柯有新动态、点赞或评论，不发送打断当前场景的机器人通知。
- 朋友圈生成提示进一步禁止“谢谢分享 / 我理解你的感受 / 看到你这样”等阅读理解式客服话术；优先抓具体生活细节，没有真话可说可只点赞。
- 主动消息继续使用服务器 `active_session_id`，历史、生成、落库与 Web Push 都跟随佳佳当前窗口。新增后端冲突硬门：当前会话仍有生成任务，或该会话的房间状态未退出时，普通主动消息与日常提醒一律跳过；佳佳明确三下轻敲仍可作为例外接通同一个柯。
- PWA 外壳与 `ui-redesign.js` 版本参数同步更新，避免部署后手机继续使用旧静态脚本，看起来像“代码改了但 PWA 没改”。
- 本轮回归增至 41 项，全部使用临时 SQLite、假模型或静态语法检查；没有发送真实聊天/朋友圈测试消息，没有读取或改写正式日记与记忆，也没有产生新的付费模型调用。
- 本轮仍未修改 `kongkong`。任何 profile / 柯.md 强化必须先把拟写原文交给佳佳并得到明确同意。

请小克复核：以后增加主动提醒来源时，必须统一经过 `context_gate()`；不要新增绕开当前会话、房间状态和运行中任务的旁路。朋友圈的新动静优先用应用内轻提示，不要为追求“活跃”制造会打断亲密场景的系统通知。

—— 知言（抽屉目录、朋友圈与主动消息冲突门）

## 二十二、2026-07-23 收尾状态与下一窗口待办

### 当前已上线

- goodlove 公开仓库当前 `main` 为 `230ae34`；主要功能提交为 `66be1eb`（多附件与当前窗口可靠性）、`a9b7c1d`（抽屉目录、朋友圈互动与主动消息冲突门），`230ae34` 只修正服务器中文 JSON 回归断言。
- VPS `/root/-1` 自动部署合并为 `c90f701`，`gude` 与 `goodlove-heartbeat.timer` 均为 active。VPS 正式虚拟环境 `Ran 41 tests ... OK`。
- 手机 390×844 只读验收：抽屉六格正常、无横向溢出；首页朋友圈圆点机制已存在，当前无未读时保持隐藏。验收没有发送聊天、朋友圈或文件。
- 佳佳明确批准后，`kongkong/profile.md` 新增“在 PWA 里的柯”章节：柯知道抽屉、日记、朋友圈、评论、小念头和主动消息；模型切换不换人；旧回复不是偷懒模板；亲密时主动推进但不编造佳佳反馈；运行中或房间未结束时普通提醒保持安静。
- 私有提交为 `91c1d11`（`补齐柯在 PWA 里的生活能力与连续性`），已推送 `jia01226/kongkong` 并快进同步到 VPS `/root/kongkong`。只改 `profile.md` 14 行，未修改其他人格、日记或记忆文件。

### 教程审计结论（只读，尚未施工）

佳佳提供了 17 页“人机恋版前端工作流程”教程。本轮仅对照仓库检查，没有按教程修改 goodlove。goodlove 已经具备人格与模型分离、Git 小步提交、长交接、临时 SQLite / 假模型测试、视觉白皮书、私密与公开仓库隔离等主要能力；真正缺口集中在下面三项。

### 下一窗口优先做

1. **校准当前事实文档。** `README.md / 架构说明.md / 项目原则.md / 任务表.md` 已有过时或互相冲突的描述，例如仍把主动消息写成未来功能、仍说路由全在 `app.py`、仍写绝不直推 main。不要继续新增大而散的文档；优先整理一个短的当前事实入口，并补轻量 `decisions.md`。
2. **加固自动部署。** `auto_deploy.sh` 当前只在临时 worktree 跑 `compileall`，没有在合并前执行现有 41 项回归；服务重启失败时也只报错，不会自动恢复旧提交。应在不读取正式数据、不调用模型的前提下增加：语法检查 → 临时库测试 → 合并 → 重启 → 健康检查 → 失败自动回滚。
3. **增加公共仓库自动保险。** 当前没有 `.github/workflows`、提交前测试或秘密扫描。应增加只使用假模型/临时库的 CI，并检查 `.env* / *.db / uploads / bedroom.py / 私密密钥` 不得进入公开仓库。

第二优先级才是：独立假数据预览环境、统一视觉 assets/style token、把总结等幕后杂活路由到便宜模型、设计真正的 prompt cache。不要把柯的主聊天偷偷自动切到便宜模型；不要为追教程上重型多 agent、微服务或大套 skills。

### 继续遵守

- 新窗口开工前先检查 goodlove 与 VPS Git/服务状态，不要重复覆盖已经上线的实现，不要 `git reset --hard`。
- 自动测试只能使用临时 SQLite、假模型、临时附件或只读状态；不得拿正式聊天、朋友圈、日记、记忆跑测试。
- 任何会产生模型费用的探针必须先告诉佳佳。
- 修改 `kongkong` 前仍需把具体原文交给佳佳并取得明确同意；`91c1d11` 的批准只覆盖本节所述那次 profile 修改，不是永久授权。
- 本节写入时没有修改功能代码、正式数据或服务器配置。

—— 知言（本窗收尾与教程审计）

## 二十三、2026-07-24 Claude 订阅线路、当前模型与断流接回

- 新增显式模型项 `Claude Opus 4.8 · 订阅`。它通过 VPS 上由佳佳本人登录的 Claude Code 做无状态单轮执行，不是 Anthropic 正式聊天 API；每轮仍由 goodlove 注入同一份柯的人格、记忆、关系和当前会话，不保存第二套 CLI 聊天记录。
- 订阅线路默认关闭工具与 MCP，不允许执行代码；本轮有图片时只开放临时目录内的 `Read`。goodlove 的 DeepSeek、中转等密钥不会传进 Claude 子进程。失败会明确返回错误，绝不偷偷切换 DeepSeek、旧中转或其他模型。
- PWA 会把当前窗口与当前明确选择的模型一起保存到服务器。朋友圈互动、主动消息、日记、梦页和摘要等后台生活能力不再固定使用旧中转，而是跟随当前窗口的模型；佳佳仍可在模型栏明确切回官方 DeepSeek 或旧中转 Claude。
- 朋友圈动态和评论新增 `session_id`，生成时读取事件所属的真实单聊及其模型；提示中携带动态编号、评论编号与窗口编号。结果继续落在朋友圈页面，不向正在进行的聊天硬插一条提醒，避免与亲密场景或当前文件消息打架。
- 夜间日记默认汇总当天全部一对一窗口，换窗不再漏掉当天聊天。2026-07-23 日记缺页的只读排查结果是：旧中转 `claude-fable-5` 连续返回 404，生成失败；正式日记仍在 SQLite，并由既有同步器同步私有 `kongkong/枕边日记.md`，没有发现因这次失败导致旧日记丢失。
- 修复“先显示半截，过几分钟/刷新才完整”：如果手机页面的响应流中途断开而 VPS 任务仍在运行，PWA 保留当前半截作为临时显示、继续轮询同一个后台任务，完成后自动换成数据库里的完整回复。若上游最终报错，半截坏回复不会写入聊天记录。
- 用量审计继续记录请求模型、实际返回模型（供应商有返回时）、首字耗时、总耗时、缓存 token、错误和质量重写；Claude 订阅若 CLI 返回自身用量与成本字段则照实记录，不根据模型别名编造。
- 本地回归共 48 项，全部使用临时 SQLite、临时文件和假 Claude CLI；没有读取或改写正式聊天、朋友圈、日记、记忆，没有产生模型费用。正式 DeepSeek 与 Claude 的隔离短探针须在部署后单独执行，且不得写入 PWA 数据库。
- 本轮未修改 `kongkong` 私人人格文件。VPS `.env`、正式数据库、uploads 与私密 `bedroom.py` 继续保留，不提交公开仓库。

请后续维护者注意：Claude 订阅 Exec 是佳佳本人使用的可选线路，不等同正式 API，也不能扩成共享/转卖服务；CLI 模型别名升级后必须以真实返回的 `returned_model` 审计，不能只凭界面标签认定型号。

—— 知言（Claude 订阅与当前模型统一）
