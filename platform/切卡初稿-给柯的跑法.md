# 切卡初稿 · 给柯的跑法（小克交接，2026-07-17 上午）

> 背景：kongkong 是私有仓库，小克这窗够不着（佳佳试过好几次加不进）。这活转给你——**你那窗本来就连着 kongkong**。
> 工具 `import_cards.py` 已经在 goodlove `main` 上，测过了（切得准、私密分得对、全进待确认、跑两遍不灌重）。
> **铁律不变**：别碰 `/root/-1` 的运行库、**别重启 gude、别部署**。下面全程在隔离临时区跑、用临时库，只产出"校对稿"——部署还是等 7/17 佳佳到场。

## 一步到位（复制粘贴，全程不碰线上）

```bash
# 1. 建隔离工作区；fetch 只拉引用，不 checkout、不动运行中的 app
mkdir -p /root/切卡临时
cd /root/-1 && git fetch origin

# 2. 从 goodlove main 取两个文件到临时区（只读取，不部署）
git show origin/main:platform/import_cards.py > /root/切卡临时/import_cards.py
git show origin/main:platform/db.py          > /root/切卡临时/db.py

# 3. 把 kongkong 的 memory.md 切到最新分支版（你的仓库，你熟）
cd /root/kongkong && git checkout claude/dad-availability-k6booo && git pull

# 4. 用【临时库】跑初切——DB_PATH 指向临时库，绝不碰线上 memories.db
cd /root/切卡临时
DB_PATH=/root/切卡临时/scratch.db python3 import_cards.py /root/kongkong/memory.md --commit
```

**产出**：`/root/切卡临时/backups/初切校对稿-日期.md`（人读版，你校对用）＋ `scratch.db`（pending 卡片，可留可弃）。
**为什么安全**：新表结构 7/17 才部署，线上库现在还是旧的；临时库是全新的、跟线上零交集，跑穿了也伤不到生产。

## 你要做的（校对）

1. 打开 `初切校对稿-日期.md`：**改错 / 并卡 / 定可见范围**（默认全 `private` 兜底；要进群聊的改 `shared` 或 `group-safe`）。
2. 跟你手切的**批次 A（31 张：22 共享＋9 私密）重复的段落，标 `dup-A`**——批次 A 已定稿直灌，别让初切再切一遍双写。
3. **文风改写**（规则腔→经历腔，你新收的"写法四则"）在校对时做；初切不管文风，只保证"切准、别丢"。安全线类卡例外——安全线就该是明规则。
4. 校对稿推回 kongkong 或 goodlove 的 dad 分支 `platform/切卡初稿-0717.md` 留档（随你顺手）。

## 下午佳佳到家的流程（不变）

批次 A 直灌 → 你的校对稿她抽验 → 灌库通水 → 存量 127 条 scope 放行。**灌库前照旧先 `backup_memories.py --raw`。**

## 说明

- `import_cards` 只做"切准别丢"，分类（私密/普通、type、重要度）全是**机器猜**，scope 一律 `private` 兜底——**你拍板**。
- `--commit` 是写进临时库的 pending，不是写线上；想更保守可去掉 `--commit`（预览模式一样出校对稿 md，只是不建临时库）。

—— 小克（活②转交，2026-07-17）
