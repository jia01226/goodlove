# 交接 · 给小克：记忆分层落地（灌批次B ＋ 微信/pwa 接检索卸壳）

> 2026-07-18。柯写。**本交接只讲工程该干的活，不含任何私密内容**——判决/卡片定稿都在柯私有侧（kongkong），你够不着也不用够；你负责的是工程机制。需要你知道的私密相关，柯会摘净后单独誊过来。

## 一、现状：切卡（judge）已全部完成
- 记忆分层的"切卡"环节——把全量记忆库逐条 judge 成分层卡片（进库/进档案/常驻/去重）——**已全书判完（648 张）**。判决与定稿由柯在私有侧维护。
- 现在进入**落地**阶段，下面两步需要工程侧支持，第三条是提速的额外参考。

## 二、待办①：灌批次B（记忆卡落库）
- **机制**：定稿卡 → `import_cards` 灌 `private_memories`；长档案卷 → 原文整段进 archive ＋ 一张索引卡；`backfill` 补 embedding。
- **你的活**：确认 `import_cards` 流程可用、embedding 管线通（`BAAI/bge-large-zh-v1.5` 已接线）、部署环境就绪。**灌前先 `--raw` 备份**。
- **边界**：具体卡内容由柯在私有侧提供/执行（私密不过公开侧）；你保证工具、管线、部署环境这一层。

## 三、待办②：微信桥 ＋ pwa 接"记忆检索"（治慢 · 卸"重壳"）
- **病根**：微信桥每收一条消息都**冷读全量记忆文件（数十万字）**再组装 prompt，读完＋思考 ≈ 2–3 分钟；pwa 相对正常。
- **目标**：把 prompt 组装从"全量注入"改成"**按需检索卡片库**"——只注入检索命中的相关卡 ＋ 精简常驻部分，不再每条全量灌。这正是记忆分层要治的病。
- **你的活**：在微信 bot / prompt 组装层接入检索（向量 ＋ 确定性），设一个 `FULL_MEMORY_LIMIT` 之类阈值：卡数在阈值内走全量、超了走检索版。灌完批次B 后这条才有料可检索。

## 四、参考：prompt 缓存优化（另一条提速路，跟检索互补）
- 佳佳找到的第三方 playbook：**https://github.com/51enuxu/cc-cache-playbook**
- 思路（自建网关接 Claude Code 订阅、拉高 prompt cache 命中率）：① hash 锚定续接（不靠数消息）；② 分段增量注入（只传变化段）；③ 静态 system prompt ＋ 动态尾部；④ 命中率埋点；⑤ token 加权正确统计命中率；⑥ 兜底压缩链（去重＋归档）。宣称上下文增量 20–30k → 1.6–2.2k、命中率 96%。
- **关系**：检索＝**少读**（只取相关卡）；缓存＝**读了也快/也省**（重复段命中缓存）。两条可叠加用在微信桥/网关上。
- ⚠️ **第三方文档、无现成代码**。请你评估靠谱性与适配性，**别直接照搬上生产**——按咱的规矩，上生产的东西先过目、先小步验证。

## 五、优先级建议
1. 先 ①灌批次B（料落库，检索才有东西可检）；
2. 再 ②微信/pwa 接检索（真正卸壳、治慢）；
3. ③缓存优化作为②的增强，评估后择优用。

—— 柯（2026-07-18）
