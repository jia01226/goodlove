# 7/17 维护窗口清单（照着走就行）

> 给佳佳的。这天咱把三件事一口气做完：**部署新记忆系统 + 微信桥接耳朵 + 换 GitHub 钥匙**。
> **铁律·绝不崩**：每一步都有"预期看到什么"和"不对劲怎么回滚"。**只要某步的"预期"没出现，就停下、别往下叠，喊柯。**
> 预计 40–60 分钟。全程柯在线陪你，拿不准随时问。
> 术语约定：下面 `<尖括号>` 里的是"占位符"——第一次做时把真实值填进去，以后就固定了。

---

## 第 0 步 · 开工前检查（三个都 ✓ 才开始）

- [ ] **电脑在手**（不是手机——这天必须坐电脑前）
- [ ] **能连上服务器**：
  ```
  ssh <服务器地址>
  ```
  **预期**：看到服务器的命令行提示符（一串 `用户@主机:~$`）。
  **不对劲**：连不上（卡住/报错）→ **今天别动，一切照常，改天再约**。连不上就不具备"出事能救"的条件，绝不硬来。
- [ ] **预留一小时**，这一小时不被打断。

进入项目目录（后面每步默认都在这里）：
```
cd <项目路径>/platform
```

---

## 第 1 步 · 备份（动任何东西之前，雷打不动）

```
./venv/bin/python backup_memories.py --raw
```
**人话**：先存一份"原样档"——真出事了，直接把这个文件换回去就回到现在，啥都不丢。
**预期**：看到两行 ✅
```
✅ 原样档已存 → .../backups/db原样档-pre-migration-....db（xx MB，出事直接换回这个文件）
✅ 已备份 N 张表、xxx 条数据 → ...
```
**不对劲**：没看到这两行 ✅（报错/没输出）→ **停，别部署**。没备份就不动库，是死规矩。喊柯看报错。

---

## 第 2 步 · 部署（拉新代码 + 重启）

**2a. 拉代码**
```
git pull
```
**预期**：看到一串文件更新，包含 `db.py`、`chat_ai.py`、`vector_search.py`、`import_cards.py`、`routes/memory.py` 等。
**不对劲**：如果冒出 `CONFLICT`（冲突）字样 → **别自己 merge，停下喊柯**（服务器直改代码正是从前酿过大战的坑）。

**2b. 重启服务**
```
<重启命令：以服务器记录为准，如 systemctl restart <服务名> ；柯现场确认>
```
**人话**：让 app 用上新代码。**新表和记忆升级会在这次重启时自动跑好**（只加东西、不动老数据，跑第几遍都一样）。
**预期**：命令没报错；手机打开 app，能正常进、能进聊天。
**不对劲 → 回滚**：app 打不开或报错，按顺序试：
1. 先把代码退回部署前：
   ```
   git reset --hard <部署前那一版的 commit 号>
   ```
   （commit 号在 `git pull` 前用 `git log -1 --format=%h` 记下来；这清单第一次做时先记好）
2. 再重启一次（同 2b 命令）。
3. 还不行 → 换回第 1 步的 `--raw` 原样档（把 `backups/db原样档-....db` 复制盖回 `memories.db`），再重启。柯陪你做。

---

## 第 3 步 · 验证三连（三个都过，才算部署成功）

**① 微信桥还活着**
```
<看桥是否在跑的命令：柯补，见第 4 步框架>
```
**预期**：桥的进程在、能收发。**不对劲**：桥掉了 → 先做第 4 步把它拉起来，再回来。

**② 单聊、群聊各发一句**
- 在 app 里给柯**单聊**发一句 → 柯正常回话。
- 到**群聊**发一句 → 成员正常回、不串味。
- **重点看隐私**：单聊里那些私密的事，**群聊里绝不能冒出来**。
**预期**：都正常、私密不外漏。**不对劲**：回话不对/私密串了 → 记下来喊柯，严重就回滚（第 2 步）。

**③ 注入日志出现新记录**
- 打开 `设置 → 开发者模式 → 记忆诊断`（或浏览器访问 `/api/memory/injection-log`）。
**预期**：看到刚才发的那几句对应的**新记录行**（有时间、用了几条记忆）。
**人话**：这说明"每轮记账"接通了，以后柯要是抽风能查账。
**不对劲**：一条新记录都没有 → 记下来告诉柯（不算致命，但要查）。

---

## 第 4 步 · 微信桥接耳朵（框架在此，细节柯现场补）

> ⚠️ 这台桥是柯修过的、脾气柯熟。下面只留**顺序框架和占位**；每个 `<...>` 的真实命令**以 kongkong 仓库里桥的记录为准，柯现场补进来**。

1. **进桥的会话**（桥跑在一个常驻会话里）：
   ```
   tmux attach -t <桥的会话名>          # 柯补：会话名 + 桥所在目录
   ```
   **预期**：看到桥的运行画面。
2. **看桥的状态 / 重连**：
   ```
   <柯补：桥的启动/重连命令>
   ```
   **预期**：桥重新连上微信、能收发。
3. **守夜人 cron 还在**（桥掉了能自动被拉起来）：
   ```
   crontab -l                          # 柯补：确认守夜人那条 cron 还在、路径对
   ```
   **预期**：看到守夜人那条定时任务。
4. **发一条真实测试**：从微信那头发一句，确认桥把话传进来、柯能回。
   **预期**：一条龙通。**不对劲**：卡在哪一步就停在哪，柯就在旁边，一起救。

> 退出 tmux 画面别按叉：先 `Ctrl+b` 松开再按 `d`（detach，桥继续在后台跑，不是关掉）。

---

## 第 5 步 · 换 GitHub 钥匙（**顺序别反：先废旧、再上新**）

1. **先在 GitHub 网页把旧钥匙废掉**：
   GitHub → Settings → Developer settings → Personal access tokens → 找到旧的 → **Revoke（废除）**。
   **人话**：先把旧钥匙作废，防它还在外面被人捡着用。
2. **生成一把新钥匙**：同一页 `Generate new token`，权限勾到够用就行，**生成后那串只显示一次，先复制好**。
3. **把新钥匙告诉服务器**：
   ```
   <更新服务器上 git 凭据的命令：柯补，如改 remote url 或 credential 存储>
   ```
   **预期**：跑一下 `git fetch` 不再要密码、能成功。
   ```
   git fetch
   ```
   **预期**：无报错、正常拉取。
   **不对劲**：新钥匙不通（旧的已废、退不回去了，别慌）→ **再生成一把新的**重来第 2–3 步。钥匙能反复生成，不是一次性的，慢慢来。

---

## 第 6 步 · 收尾巡检（关窗口前，扫一遍）

- [ ] app 各个页面点一遍（聊天/日记/朋友圈/记忆），都正常。
- [ ] 再单聊、群聊各发一句，确认还顺。
- [ ] 备份定时任务还在：
  ```
  crontab -l          # 预期：看到每天备份那条 cron
  ```
- [ ] 微信桥确认活着（第 4 步 detach 出来后，从微信再发一句试试）。
- [ ] 都 ✓ → **收工**。今天动过的：代码新版、微信桥、GitHub 钥匙，三样都验过。

---

## 出任何问题的总原则（护身符）

1. **某步"预期"没出现 → 停，别往下叠。** 叠着做只会越缠越乱。
2. **最硬的回滚**：换回第 1 步的 `--raw` 原样档 ＋ `git reset` 到部署前那一版 ＋ 重启。回到今早，啥都没丢。
3. **柯全程在线**：拿不准、看不懂、手抖了，问柯，别自己扛。这天的底气就是"有人在场随时能救"。

—— 清单：小克拟（2026-07-15）；微信桥细节：柯补
